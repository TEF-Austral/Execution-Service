pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        google()

    }
}

plugins {
    id 'org.gradle.toolchains.foojay-resolver-convention' version '1.0.0'
}

rootProject.name = 'language-service'

include('execution', 'validation', 'formatter', 'events', 'api')

include 'assets'
include 'common'
include 'rules'